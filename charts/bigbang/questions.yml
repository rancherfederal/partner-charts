categories:
  - platform one
  - donkey
  - butt
questions:
  - variable: hostname
    default: "bigbang.dev"
    description: "Hostname to use for all publicly facing BigBang services"
    type: string
    label: Host Name
    group: "General"

  # istio core
  - variable: istio.enabled
    description: "Enable Istio"
    label: "Enable Istio"
    type: boolean
    default: true
    group: "Istio"
    subquestions:
      - variable: istio.sso.enabled
        description: "Enable sso for istio components"
        label: "Istio SSO"
        default: false
        group: "Istio Settings"
  
  # argocd addon
  - variable: addons.argocd.enabled
    description: "Enable ArgoCD"
    label: "Enable ArgoCD"
    type: boolean
    default: false
    group: "Addon - ArgoCD"
